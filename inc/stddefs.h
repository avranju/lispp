#ifndef STDDEFS_H
#define STDDEFS_H

#include <stdlib.h>
#include <stdio.h>
#include <string.h>

#ifndef min
#define min(a,b) (((a) < (b)) ? (a) : (b))
#endif

#ifndef max
#define max(a,b) (((a) > (b)) ? (a) : (b))
#endif

#endif // !STDDEFS_H
